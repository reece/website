# AGENTS.md

## Overview

This repository is a Nuxt 4 website with content-driven pages, custom Vue components under `app/`, and Vitest coverage for UI primitives. See [docs/development.md](docs/development.md) for stack details, repository layout, working rules, and validation expectations — that document is the shared source of truth for humans and agents.

## Agent-specific guardrails

- Avoid creating extra planning markdown files unless explicitly requested.
- Add documentation consistent with existing components.
- Do not introduce secrets or environment-specific values into tracked files.
